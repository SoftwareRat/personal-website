{
  "name": "softwarerat-tech",
  "version": "1.0.0",
  "description": "SoftwareRat's personal website and blog",
  "main": "scripts/build.js",
  "scripts": {
    "build": "node scripts/build.js && node scripts/generate-rss.js",
    "dev": "node scripts/dev.js",
    "clean": "rm -rf public/*",
    "production": "npm ci && npm run build"
  },
  "dependencies": {
    "chokidar": "^3.5.3",
    "express": "^4.21.2",
    "front-matter": "^4.0.2",
    "fs-extra": "^11.2.0",
    "handlebars": "^4.7.8",
    "highlight.js": "^11.9.0",
    "marked": "^4.3.0"
  },
  "devDependencies": {
    "prettier": "^3.0.3"
  }
}
